if (!hasinterface) then {
    if (debug) then {
        hint "is this it?";
    };
    gameready = true;
    // generate finish list/ checkpointlist
    
    // wait for groups to get to start points
    
    // start countdown
    
    // Gamestart = true;
    
    // wait for all groups to finish
};
